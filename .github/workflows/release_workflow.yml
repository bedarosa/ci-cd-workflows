name: Release Workflow

on:
    pull_request:
        types:
            - closed
        
jobs:
    find-diference:
        uses: ./.github/workflows/detect_changes.yml

    echo-test:
        needs: [find-diference]
        uses: ./.github/workflows/echo_test.yml